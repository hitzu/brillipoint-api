# .github/workflows/pipeline-main.yml
name: pipeline-main

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: pipeline-main-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NODE_VERSION: 22
      PNPM_VERSION: 9
      # Database configuration for tests
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      DB_NAME: bookandsign_test
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: bookandsign_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node (+ pnpm cache)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run the full migration chain against an empty database
        env:
          NODE_ENV: ci
        run: pnpm run db:run

      - name: Show applied migrations
        env:
          NODE_ENV: ci
        run: pnpm run db:status

      - name: Verify the schema converges with the entity model
        env:
          NODE_ENV: ci
        run: |
          set +e
          output=$(pnpm run db:gen 2>&1)
          set -e
          echo "$output"
          if echo "$output" | grep -q "No changes in database schema were found"; then
            echo "::notice::Migration-built schema matches the entity model."
            exit 0
          fi
          echo "::error::The migration chain and the entity model disagree. \
          A migration was generated against a database that the chain had just \
          built, which means the two paths to the schema have diverged. \
          Run 'pnpm run db:gen' locally and add the missing migration."
          exit 1

      - name: Build
        run: pnpm run build

      - name: Run unit tests
        env:
          NODE_ENV: test
          DB_HOST: ${{ env.DB_HOST }}
          DB_PORT: ${{ env.DB_PORT }}
          DB_USERNAME: ${{ env.DB_USERNAME }}
          DB_PASSWORD: ${{ env.DB_PASSWORD }}
          DB_NAME: ${{ env.DB_NAME }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
        run: pnpm run test

  e2e-bruno:
    name: e2e-bruno
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: test
    env:
      NODE_VERSION: 22
      PNPM_VERSION: 9
      NODE_ENV: ci
      PORT: 3000
      # Database configuration for the e2e run (own Postgres service, isolated
      # from the "test" job's database).
      DB_HOST: localhost
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: postgres
      DB_NAME: bookandsign_e2e
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: bookandsign_e2e
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node (+ pnpm cache)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run the full migration chain against an empty database
        run: pnpm run db:run

      - name: Build
        run: pnpm run build

      - name: Start the app in the background
        run: |
          nohup node dist/src/main.js > app.log 2>&1 &
          echo $! > app.pid
          disown

      - name: Wait for the app to be reachable
        run: |
          for i in $(seq 1 30); do
            if curl -sf "http://localhost:${PORT}/bookings/calendar?year=2026&month=1" > /dev/null; then
              echo "App is reachable after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "::error::App did not become reachable within 30s"
          cat app.log
          exit 1

      - name: Run the Bruno e2e collection (bookings + notes + auth boundary)
        run: pnpm run test:e2e:bruno -- --env-var base_url=http://localhost:${{ env.PORT }}

      - name: Show app logs
        if: always()
        run: cat app.log || true

      - name: Stop the app
        if: always()
        run: |
          if [ -f app.pid ]; then
            kill "$(cat app.pid)" 2>/dev/null || true
          fi

  migrate:
    name: migrate
    if: github.event_name != 'pull_request'
    needs: test
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 20
    env:
      NODE_VERSION: 22
      PNPM_VERSION: 9
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node (+ pnpm cache)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Debug - Check SUPABASE_DB_URL
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          if [ -z "$SUPABASE_DB_URL" ]; then
            echo "SUPABASE_DB_URL is EMPTY or NOT SET"
            echo ""
            echo "TROUBLESHOOTING:"
            echo "1. Verify the secret 'SUPABASE_DB_URL' exists in the 'production' environment"
            echo "2. Go to: Settings > Environments > production > Secrets and variables"
            echo "3. Make sure 'SUPABASE_DB_URL' is added as a secret (not a variable)"
            echo "4. If using repository secrets instead, remove 'environment: production' from the job"
            exit 1
          else
            echo "SUPABASE_DB_URL is SET"
          fi

      - name: migrations:status (before)
        env:
          NODE_ENV: production
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: pnpm run db:status

      - name: migrations:run
        env:
          NODE_ENV: production
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: pnpm run db:run

      - name: migrations:status (after)
        env:
          NODE_ENV: production
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: pnpm run db:status

  deploy:
    name: deploy
    if: github.event_name != 'pull_request'
    needs: migrate
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    steps:
      - name: Trigger Render Deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" \
            -H "Content-Type: application/json" \
            || echo "Deploy webhook triggered"
