meta {
  name: get event by id
  type: http
  seq: 9
}

get {
  url: {{base_url}}/events/1
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

vars:pre-request {
  event_key: 26-04-26
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  v1: unchanged payload, still includes the deprecated schedule fields
  (see the `events` folder docs). Prefer `v2/get event by id` for a
  response whose schedule/status come only from the contract's EVENT
  booking.
}
