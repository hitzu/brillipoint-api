meta {
  name: create overnight booking for agenda
  type: http
  seq: 11
}

post {
  url: {{base_url}}/bookings/internal
  body: json
  auth: inherit
}

headers {
  authorization: Bearer {{token}}
}

body:json {
  {
    "scheduleType": "exact",
    "eventDate": "2026-12-22",
    "serviceStartsAt": "2026-12-22T22:00:00-06:00",
    "serviceEndsAt": "2026-12-23T02:00:00-06:00",
    "title": "Agenda overnight fixture",
    "purpose": "other",
    "venueName": "Night hall"
  }
}

vars:post-response {
  agenda_overnight_booking_id: res.body.id
}

tests {
  test("creates the overnight fixture booking", function () {
    expect(res.getStatus()).to.equal(201);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
