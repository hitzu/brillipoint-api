meta {
  name: create note (401 - missing token)
  type: http
  seq: 15
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

body:json {
  {
    "scope": "slot",
    "targetId": 1,
    "kind": "internal",
    "content": "Missing token"
  }
}

tests {
  test("rejects the note creation with no Authorization header", function () {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


