meta {
  name: create note (400 - invalid targetId type)
  type: http
  seq: 14
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "targetId": "1",
    "kind": "internal",
    "content": "targetId must be a number"
  }
}

tests {
  test("rejects a string targetId with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


