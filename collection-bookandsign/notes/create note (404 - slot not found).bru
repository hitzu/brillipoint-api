meta {
  name: create note (404 - slot not found)
  type: http
  seq: 9
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "targetId": 999999,
    "kind": "internal",
    "content": "Should fail because slot does not exist"
  }
}

tests {
  test("rejects the note with 404 when the slot does not exist", function () {
    expect(res.getStatus()).to.equal(404);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


