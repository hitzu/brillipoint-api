meta {
  name: get notes (400 - invalid scope)
  type: http
  seq: 4
}

get {
  url: {{base_url}}/notes/invalid/1
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("rejects an unknown note scope with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


