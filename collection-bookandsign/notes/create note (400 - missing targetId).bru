meta {
  name: create note (400 - missing targetId)
  type: http
  seq: 11
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "kind": "internal",
    "content": "Missing targetId"
  }
}

tests {
  test("rejects a note with no targetId with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


