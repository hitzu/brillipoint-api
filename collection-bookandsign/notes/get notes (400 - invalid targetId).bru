meta {
  name: get notes (400 - invalid targetId)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/notes/slot/abc
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("rejects a non-numeric targetId with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


