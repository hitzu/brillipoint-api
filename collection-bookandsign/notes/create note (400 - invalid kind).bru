meta {
  name: create note (400 - invalid kind)
  type: http
  seq: 13
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "targetId": 1,
    "kind": "invalid",
    "content": "Invalid kind"
  }
}

tests {
  test("rejects an unknown note kind with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


