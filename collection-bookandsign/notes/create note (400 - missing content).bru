meta {
  name: create note (400 - missing content)
  type: http
  seq: 10
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "slot",
    "targetId": 1,
    "kind": "internal"
  }
}

tests {
  test("rejects a note with no content with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


