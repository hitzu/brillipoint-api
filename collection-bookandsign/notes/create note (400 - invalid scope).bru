meta {
  name: create note (400 - invalid scope)
  type: http
  seq: 12
}

post {
  url: {{base_url}}/notes
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "scope": "invalid",
    "targetId": 1,
    "kind": "internal",
    "content": "Invalid scope"
  }
}

tests {
  test("rejects an unknown note scope with 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


