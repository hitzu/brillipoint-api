meta {
  name: get notes (401 - missing token)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/notes/slot/1
  body: json
  auth: inherit
}

tests {
  test("rejects listing notes with no Authorization header", function () {
    expect(res.getStatus()).to.equal(401);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}


